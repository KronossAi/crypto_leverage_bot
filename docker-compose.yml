version: "3.9"

services:
  trading-bot:
    build: .
    container_name: crypto_leverage_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./config:/app/.config
